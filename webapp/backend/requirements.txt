fastapi>=0.139
uvicorn[standard]>=0.50
pillow>=12.0
numpy
scipy
matplotlib
pydantic>=2.0
python-multipart
python-docx
invisible-watermark

# Installed separately with the CPU-only index:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch
torchvision

# Optional - real face alignment instead of center-crop fallback.
# Needs internet + a working TensorFlow install; skip if you're fine with
# the center-crop fallback for local testing.
#
# On Windows: do NOT just uncomment these two lines and pip install -r
# this file - `retina-face` depends on full `opencv-python`, which
# conflicts with `opencv-python-headless` above and breaks on Windows N
# editions (missing Media Foundation DLLs). Install manually instead -
# see DeveloperGuide.md Section 9.1 for the verified working recipe:
#   pip install opencv-python-headless
#   pip install --no-deps retina-face
#   pip install gdown Pillow tensorflow
# retina-face
# opencv-python
