fastapi
uvicorn[standard]
pillow
numpy
scipy
matplotlib
pydantic>=2.0
python-multipart
python-docx

# torch + torchvision installed separately in Dockerfile (CPU wheel).
# Optional face alignment (not on Render free tier):
# retina-face
# opencv-python
